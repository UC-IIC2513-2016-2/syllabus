<%= form_for(trojan, remote: true, data: { type: :html }) do |f| %>
  <% if trojan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trojan.errors.count, "error") %> prohibited this trojan from being saved:</h2>

      <ul>
      <% trojan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
